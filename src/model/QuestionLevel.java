package model;

/**
 * רמת קושי של *שאלה* (לא של המשחק עצמו).
 */
public enum QuestionLevel {
    EASY,      // שאלה קלה
    MEDIUM,    // שאלה בינונית
    HARD,      // שאלה קשה
    EXPERT     // שאלת מומחה
}
